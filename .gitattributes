# 여러 PC에서 작업할 때 줄바꿈이 뒤섞이지 않도록 저장소에는 LF로 통일한다.
* text=auto eol=lf

# 윈도우에서 실행되는 스크립트는 CRLF를 유지한다.
*.ps1 text eol=crlf
*.cmd text eol=crlf
*.bat text eol=crlf

# 바이너리는 손대지 않는다.
*.png binary
*.jpg binary
*.ico binary
*.xlsx binary
*.hwp binary
