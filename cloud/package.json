{
  "name": "fki-survey-cloud",
  "version": "1.0.0",
  "private": true,
  "description": "FKI 설문 시스템 — Cloudflare Pages + Supabase 배포판",
  "scripts": {
    "dev": "wrangler pages dev public",
    "deploy": "wrangler pages deploy public --project-name fki-survey"
  },
  "devDependencies": {
    "wrangler": "^3.114.14"
  }
}
